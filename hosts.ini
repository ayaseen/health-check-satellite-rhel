# Health Check Hosts Configuration File
# This file defines the hosts to check and their connection parameters

[defaults]
# SSH connection settings
ssh_user = ayaseen
ssh_port = 22
ssh_timeout = 30  # Increased timeout for remote commands
password = X22blue123@813

# SSH key file (optional - will try this first before password)
# ssh_key_file = ~/.ssh/id_rsa

# Parallel execution settings
parallel_connections = 3

# Privilege escalation settings (IMPORTANT for remote execution)
become = yes
become_method = sudo
become_user = root
become_pass = X22blue123@813

# Report settings
report_format = consolidated

[rhel_hosts]
# List your RHEL hosts here
192.168.1.100
;192.168.1.101
;192.168.1.102

# You can override settings per host:
# 192.168.1.103 user=root password=different_password
# 192.168.1.104 port=2222
# 192.168.1.105 become=no

[satellite_hosts]
# List your Satellite servers here
# sat1.example.com
# sat2.example.com user=admin